<%= simple_form_for(:post,url: admin_posts_path,:html => {novalidate: true, multipart: true}) do |f| %>
  <%= f.error_notification %>

  <div class="form-group">
    <%= f.input :title, input_html:{class:'form-control'} %>
  </div>

  <div class="field">
    <%= f.file_field :image %>
    <%= f.hidden_field :image_cache %>
  </div>

  <div class="form-group">
     <%=f.select :category_id, @categories, :prompt=>"请选择"%> 
  </div>
  <div class="form-group">
    <div class="controls">
      <label class="checkbox">是否为活动</label>
      <input type="checkbox" value="true" name="is_activity" checked="checked">
    </div>
  </div>
  <div class="form-group">
    <%= f.input :content, class:'summernote' %>
  </div>

  <div class="form-group">
    <%= f.button :submit, class:'btn btn-default' %>
  </div>
<% end %>